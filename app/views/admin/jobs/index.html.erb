<h1>All Jobs</h1>
<% @jobs.each do |job|%>
  <div>
    <%= link_to job.name.capitalize, admin_job_path(job) %>
    <br>
    Has <%= pluralize( job.characters.count, 'Character') %> with this Job
  </div>
  <br>
<% end %>
